# Install the CPU torch wheel from https://download.pytorch.org/whl/cpu first.
# CUDA wheels can be used instead on a host with sufficient free GPU memory.
torch==2.14.0
transformers==4.57.6
safetensors==0.8.0
sentencepiece==0.2.1
hf-xet==1.6.0
